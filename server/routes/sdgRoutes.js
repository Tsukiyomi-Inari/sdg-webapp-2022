//  Routing for SDG API handling HTTP requests to (GET) fetch, (PUT) update/replace, display data(POST)

/* const expess = require('express')
const router = express.Router()
const {
    getSDG,
    setSDG,
    updateSDG,
} = require('../controller/sdgController') */